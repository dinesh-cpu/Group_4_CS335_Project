int main()
{
    // naive data type
    int a;
    bool b = FALSE;
    char c = 'ab';
    int i;
    int arr[6] = {3, 4, 5, 6, -3, 100};

    /* var and expr */
    int temp = 1 + 2;

    // conditional
    if (temp < 5)
    {
        if (a == 2)
        {
            a = 3;
        }
    }
    else
    {
        a = 3;
    }
    // loops
    for (i = 0; i < 10; i++)
    {
        int temp = 5;
    }
    // while
    while (i < 20)
    {
        i++;
    }

    // i/o
    printf("hello %v %d ", i, temp);
    scanf("%d", &temp);

    // arrays
}
